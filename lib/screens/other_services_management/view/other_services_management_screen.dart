import 'package:flutter/material.dart';

import '../../../utils/app_styles.dart';

class OtherServicesManagementScreen extends StatelessWidget {
  const OtherServicesManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
        return  Center(
          child: Text('Other Services Management Screen',style: AppStyles().normalTextStyle,),
        );
  }
}